# Research

This folder is reserved for source notes, papers, extracts, and exploratory analyses that inform the conjecture but are not themselves canonical inputs.

Canonical prose belongs in `intent/`. Structured CSV evidence belongs in `data/sources/`, workbooks belong in `data/reports/`, and audience-facing code belongs in `surfaces/`.
