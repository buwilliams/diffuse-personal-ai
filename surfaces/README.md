# Surfaces

Surfaces render the canonical argument and data for an audience.

- `website/` is the interactive public countdown.
- `report-cards/` contains the capability and compute workbook builders plus the HTML-table generator.

Change `intent/` and `data/` first, then regenerate or verify the affected surface.
